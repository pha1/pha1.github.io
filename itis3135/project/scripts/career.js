$(document).ready(function() {
    /* Accordion Widget is loaded into the element with "accordion" id. The accordion's setting is set to collapsible
     and the height of the accordion is matched with the content provided. */
    $("#accordion").accordion({
        collapsible: true,
        heightStyle: "content"
    });
});